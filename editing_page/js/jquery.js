
/*function regular() {
   console.log("hi") ;
}
$(function(){
function changeFont(range, fontWeight,fontStyle ,fontFamily, textDeco){
    fontFamily = fontFamily || "\"Open Sans\", Arial, sans-serif";
    fontWeight = fontWeight || "normal";
    fontStyle = fontStyle || "none";
    textDeco = textDeco || "none";
let span = document.createElement("span");

span.style.cssText = 
"font-weight: "+fontWeight+";"
"font-family: "+fontFamily+";";
span.appendChild(range.extractContents());
range.insertNode(span);
// kjifejlsjfiljlsejfisl
$(".bold")
.mousedown(function(){
let range = window.getSelection().getRangeAt(0);
let span = document.createElement("b");
span.appendChild(range.extractContents());
range.insertNode(span);

})
$(".italic").mousedown(function(){
let range = window.getSelection().getRangeAt(0);
let span = document.createElement("i");
span.appendChild(range.extractContents());
range.insertNode(span);

})
$(".strike").mousedown(function(){
let range = window.getSelection().getRangeAt(0);
let span = document.createElement("del");
span.appendChild(range.extractContents());
range.insertNode(span);
console.log($(".contianer").html());
})
$.post("test.php", {name: "mahdi"})
})*/